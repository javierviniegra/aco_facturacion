{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% block field %}
    <table class="table table-responsive">
    <th>
        <td># de Tarjeta</td>
        <td>Vigencia</td>
        <td>Fotografía</td>
    </th>
    {% if value is not empty %}
        <tr>
            <td></td>
            <td>{{ value.numero }}</td>
            <td>{{ value.vigencia|date('d-m-Y H:i:s')  }}</td>
            <td><a href="{{ vich_uploader_asset(value,'fotografiaFile') }}" target="_blank">Descargar</a></td>
        </tr>
    {% else %}
        <tr>Sin Campos</tr>
    {% endif %}
    </table>
{% endblock %}